const CATEGORIES = ["tshirt", "trackpant"];

const ORDER_STATUSES = [
  "pending",
  "order placed",
  "shipped",
  "delivered",
  "cancelled",
  "failed",
];

const PAYMENT_STATUSES = ["pending", "paid", "failed"];

module.exports = {
  CATEGORIES,
  ORDER_STATUSES,
  PAYMENT_STATUSES
};
